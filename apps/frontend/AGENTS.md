# Frontend

- 변경 전 현재 파일과 `package.json` 확인.
- API 호출: `lib/api.ts`.
- 주요 화면: `components/archive-shell.tsx` 상태 흐름 준수.
- UI: `components/ui/` shadcn/Radix 우선.
- 확인: `npm run lint`; 필요 시 `npm run build`.
