# 검색 필터 확장 TODO

## 범위

키워드, 의미 검색, RAG 검색에 공통 필터를 추가합니다.

## 백엔드

- 현재 필터는 `folder_id`, `root_only`, `limit`입니다.
- 문서 유형, 태그, 날짜, 처리 상태 필터가 없습니다.
- 필터 확장 시 keyword, semantic, rag 검색의 조건을 일관되게 맞춰야 합니다.
- 필요한 관계형 인덱스도 함께 검토해야 합니다.

## 프론트엔드

- 현재 검색은 선택 폴더 중심이고 키워드/의미/RAG 모드를 지원합니다.
- 문서 유형, 태그, 날짜, 상태 필터 UI가 없습니다.
- 필터 선택 상태와 검색 결과 초기화 흐름이 필요합니다.
