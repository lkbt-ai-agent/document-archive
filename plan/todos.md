# TODOS
아래 내용은 좀 더 자세한 todo.md 로 정리되어야 한다. 그 전까지는 구현하지 않는다.
## high
0. 프로젝트 루트 scripts 폴더의 스크립트들의 역할 및 사용 여부 검증.
1. md 파일류에서 아키텍처 스펙을 명시할 때, 버전도 포함시킬 것
2. 텍스트 확장자 문서는 "원본 보기" 버튼 클릭 시, 마크다운 뷰어 UI 를 제공해야 한다.
3. 인물, 기관, 날짜 등 메타 데이터 프로퍼티에 대한 추가 및 구체화 계획 수립 (notion 등 타사 서비스 사례, reddit 등 개발자 구현 사례 참고. 사용자가 RAG 메타데이터를 보정하는 기능은 또 어떤지?)
4. 키워드/시맨틱/RAG 검색 시, 디버깅 방안 필요 (백엔드 쿼리 로깅, UI 에 해당 키워드와 일치하는 메타데이터 혹은 청크가 무엇인지 표기 등...)
5. 2차 산출물 기능 추가 설계: 마크다운 기반 통계 그래프 보고서 작성 기능. 스펙과 구현 플로우 설계 필요.
6. 문서 표지: 예전 미카이브의 썸네일 자동 생성 기능처럼 고려 (반응형 카드형 그리드 UI 를 설계한 다음, 썸네일 생성 모델 및 작업 플로우에 대한 설계 필요)
7. 폴더 및 파일/RAG/Minio/RDB 데이터를 백업하고, 필요할 때마다 물리 삭제 및 생성할 수 있는 대책 강구 (스크립트를 돌릴지, 배치를 활용할지, 별도 라이브러리를 사용할지)
## medium
1. (데이터 모집 후 구체화 필요) 요약~문서 병합 기능 성능, 처리 용량 기준, 튜닝 포인트 정의 및 점검 (local llm vs aws 배드락 기준)
2. (데이터 모집 후 구체화 필요) RAG 색인 기능 성능, 처리 용량 기준, 튜닝 포인트 정의 및 점검 (local llm vs aws 배드락 기준)
3. 휴지통 기능 설계: 예를 들어 휴지통에 들어간 폴더의 하위 폴더/파일은 7일 뒤 물리 삭제. 현재 구현된 삭제 기능도 어디까지 논리/물리 삭제인지 분석할 것.
4. 폴더와 문서의 validation 설계: 예를 들어, 처리중인 문서는 다른 폴더로 이동시켜도 되는가 아니면 이동시키면 안되는가? 혹은 처리중인 문서가 존재하는 폴더는 이름 변경이나 이동 및 rename 이 가능한가? 현재 구현 플로우에서 어느 기능이 충돌하면 안되는지 분석.
## low
1. 좌측 사이드바: 폴더 그루핑 생성 혹은 rename 시, 의미에 적합한 icon 찾거나 생성하는 기능 (lucide-icon 에서 찾거나, icon 생성 로컬 모델 필요)
2. 로그 정리: 현재 백엔드 로깅의 한계를 분석하고, 파이썬 로깅 라이브러리를 적용하거나 "한글화" 등 가시성 높이기 위한 방안 설계.
3. (후순위) 메일을 SMTP 혹은 메일링 서비스 API 로 전달받아, 메일 요약본 만들기 기능